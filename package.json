{
  "name": "con-task-runner",
  "version": "1.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && tsc -p .",
    "dev": "npm run build -- --watch",
    "prepublish": "npm build"
  },
  "devDependencies": {
    "typescript": "^3.6.2"
  }
}
